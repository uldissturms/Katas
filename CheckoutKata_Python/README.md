Checkout kata in Python
--------------------------------------
`virtualenv env` create virtual environment

`. ./env/bin/activate` activate virtualenv

`pip install -r requirements.txt` install requirements

`nosetests` to run tests

